#!/bin/bash

ROOTDIR="$1"

cd $ROOTDIR/server

$ROOTDIR/bin/server-static $ROOTDIR

printf "[*] Creating and running migrations\n\n"
python manage.py makemigrations
python manage.py migrate

python manage.py runserver 0.0.0.0:7200
